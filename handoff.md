# 采购数据处理（大项目）— 交接

> 这是一个**父级大项目**，本身不含代码，只做总管：统一归口采购相关的多个子项目。
> 给下一个 Agent / 下一台设备快速接手用。保持短、准、最新。

## 定位

`采购数据处理` 是采购数据方向的**总管项目**，下辖若干独立子项目。各子项目有各自的代码仓库、文档和进度；本项目只维护全局视图与归口关系。

## 子项目清单

| 子项目 | 原名 | 状态 | 代码位置 | 文档 |
|--------|------|------|----------|------|
| 季度图表总结 | procurement-charts | 已完成（Q1，待 Q2 数据） | `D:\workspace\procurement-charts` | `Jeff1993/procurement-charts/` |

> 新增子项目时：在此表登记，并在根 `Jeff1993/.project-status.yaml` 的 `采购数据处理.subprojects` 下追加。

## 约定

- **代码默认位置**：`D:\workspace`（新子项目一律建在此；旧的 `D:\ClaudeCode` 已弃用为默认）。
- **命名**：物理目录 / 仓库名用英文 kebab-case；显示名可用中文。
- **层级登记**：子项目在根 SSOT 里加 `parent: 采购数据处理`；父项目用 `subprojects:` 列出。

## 下一步

1. 季度图表总结：等业务方提供 Q2 数据后接入（当前唯一阻塞点在数据侧）。
2. 视需要新增其它采购子项目（如数据清洗、报表导出等），建在 `D:\workspace` 下并登记。

## 阻塞点

- 季度图表总结：等待 Q2 数据。

## 历史

- 2026-07-01：重组。原独立空壳「采购数据处理」升格为大项目总管；`procurement-charts` 更名「季度图表总结」并从 `D:\ClaudeCode` 迁至 `D:\workspace`，纳入本项目管辖。
- 2026-05-09：初始化「采购数据处理」空壳（规划中，未落地代码）。
